namespace ModelSaber.Main
{
    public class Constants
    {
        public const string TempDirectory = "temp";
        public const int UploadSleepTime = 60 * 1000;
        public const bool UploadEnabled = false; //TODO set to true when storage bucket logic is written and mode is in release
    }
}
